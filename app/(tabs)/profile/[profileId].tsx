import Profile from "@/components/_root/profile/Profile";
import Layout from "@/components/ui/Layout";

const ProfileScreen = () => {
  return (
    <Layout className="!px-0">
      <Profile />
    </Layout>
  );
};

export default ProfileScreen;
