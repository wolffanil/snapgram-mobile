import Saved from "@/components/_root/saved/Saved";
import Layout from "@/components/ui/Layout";

const savePosts = () => {
  return (
    <Layout className="!px-0">
      <Saved />
    </Layout>
  );
};

export default savePosts;
